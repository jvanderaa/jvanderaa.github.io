# config/_default/config.toml

baseURL = "https://josh-v.com"
languageCode = "en"
googleAnalytics = "G-QCDLSTK9K6"
# Theme is now managed via Hugo modules in module.toml
# theme = ["congo", "hugo-admonitions"]

[taxonomies]
  tag = "tags"
  category = "categories"

[pagination]
  pagerSize = 20

[permalinks]
  posts = "/:slug"

[outputFormats.RSS]
  mediaType = "application/rss+xml"
  baseName = "index"

[outputs]
  home = ["HTML", "RSS", "JSON"]
